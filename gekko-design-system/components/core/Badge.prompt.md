**Badge** — small fully-rounded pill. `solid` is the accent-filled badge used for "Популярный выбор" (often with a star icon); `soft` is a tinted variant; `outline` is the quiet version.

```jsx
<Badge icon={<Star />}>Популярный выбор</Badge>
<Badge variant="soft">Новинка</Badge>
```
