**Chip** — a small selectable tag used for the "Что вас интересует?" multi-select. Quiet bordered default; gains a strong border + tinted fill when `active`.

```jsx
<Chip active={selected} onClick={toggle}>Оптовый заказ</Chip>
```
