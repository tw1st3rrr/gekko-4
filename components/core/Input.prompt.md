**Input** — rounded-xl field with hairline border, transparent fill and a dark focus ring. Use `as="textarea"` for the message box.

```jsx
<Input placeholder="Имя" />
<Input as="textarea" rows={4} placeholder="Что хотите создать..." />
```
