**Card** — the brand's default container. Soft 16px radius, hairline border, subtle shadow. Pass `interactive` for the hover-lift used on step/news cards; pass `popular` for the accent-bordered pricing card.

```jsx
<Card interactive>…</Card>
<Card popular>…</Card>
```
Cards in the brand are never sharp-cornered and never use a single colored left border.
